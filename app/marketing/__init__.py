"""Módulo de campañas y avisos de marketing (Fase 6).

- `optin.py`: regla simple de opt-in/opt-out por palabra clave en
  conversación (configurable por tenant), más el criterio de conversión
  prospect → client.
- `campaigns.py`: servicio de campañas (crear, estimar, lanzar, dispatch
  con pacing, statuses del webhook, métricas y costeo).
"""
